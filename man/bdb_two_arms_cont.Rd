% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bdb_two_arms_cont.R
\name{bdb_two_arms_cont}
\alias{bdb_two_arms_cont}
\title{Bayesian Dynamic Borrowing for Two-Arm Trial with Continuous Outcome}
\usage{
bdb_two_arms_cont(
  Y,
  Z,
  TRT,
  X,
  A,
  s = 3,
  a = 1,
  za = 0.05,
  n_iter = 5000,
  double_adj = TRUE,
  alpha = 0.01,
  beta = 0.01,
  tau = 1e-05,
  show_plots = FALSE
)
}
\arguments{
\item{Y}{Numeric vector of continuous outcomes.}

\item{Z}{Numeric vector indicating data source: 1 for current trial data, 0 for external data.}

\item{TRT}{Numeric vector indicating treatment: 1 for active treatment, 0 for control.}

\item{X}{Matrix or data frame of baseline covariates (confounders).}

\item{A}{Numeric. Number of subjects intended to be borrowed from external data source.}

\item{s}{Integer. Number of strata for propensity score stratification (default 3).}

\item{a}{Numeric. Tuning parameter for the arctangent elastic function (default 1).}

\item{za}{Numeric. Significance level for credible intervals (default 0.05 for 95\% CrI).}

\item{n_iter}{Integer. Number of MCMC iterations (default 5000).}

\item{double_adj}{Logical. If TRUE (default), both baseline and outcome adjustment; if FALSE, baseline only.}

\item{alpha}{Numeric. Shape hyperparameter for Gamma prior on precision (default 0.01).}

\item{beta}{Numeric. Rate hyperparameter for Gamma prior on precision (default 0.01).}

\item{tau}{Numeric. Precision for Normal prior on mean (default 1e-5).}

\item{show_plots}{Logical. If TRUE, returns diagnostic plots in \code{plots} element (default FALSE).}
}
\value{
A list with \code{N0_t}, \code{n_s0}, \code{n_s1}, \code{n_s11}, \code{n_s10}, \code{r_s0}, \code{r_s10},
  \code{r_ica}, \code{r_eca}, \code{A_eff}, \code{v}, \code{r}, \code{gamma}, \code{PPP}, \code{gPPP},
  \code{mean.theta}, \code{var.theta}, \code{CrI}, \code{Width}, \code{theta_s}, \code{theta_o}, \code{theta} (same as \code{theta_o}),
  and optionally \code{plots} when \code{show_plots = TRUE}.
}
\description{
Applies the Bayesian Dynamic Borrowing double-adjustment method when the control arm
of a randomized trial is augmented with external data (continuous outcome). Requires JAGS.
}
\examples{
\dontrun{
# Requires rjags and JAGS.
# See vignette("Introduction to BayesDDB", package = "BayesDDB") for full examples.
}
}
\references{
Farjat, A., Ji, Y., Kaiser, A., Lu, C., Pap, A., Potts, J., Wang, M. A Comprehensive Bayesian Double-Adjustment
Approach to Dynamic Borrowing of External Data. Manuscript in progress.
Wang, C., Li, H., Chen, W.C., Lu, N., Tiwari, R., Xu, Y. and Yue, L.Q., 2019. Journal of biopharmaceutical statistics, 29(5), pp.731-748.
}
\seealso{
\code{\link{bdb_single_arm_bin}}, \code{\link{bdb_single_arm_cont}}, \code{\link{bdb_two_arms_bin}}, \code{\link{elastic_function}}
}
