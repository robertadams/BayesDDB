% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bdb_single_arm_bin.R
\name{bdb_single_arm_bin}
\alias{bdb_single_arm_bin}
\title{Bayesian Dynamic Borrowing for Single-Arm Trial with Binary Outcome}
\usage{
bdb_single_arm_bin(
  Y,
  Z,
  X,
  A,
  s = 3,
  a = 1,
  alpha = 0.5,
  beta = 0.5,
  za = 0.05,
  K = 10000,
  double_adj = TRUE,
  show_plots = FALSE
)
}
\arguments{
\item{Y}{Numeric vector of binary outcomes (0s and 1s).}

\item{Z}{Numeric vector indicating data source: 1 for current trial data,
0 for external data.}

\item{X}{Matrix or data frame of baseline covariates (confounders) with
n rows and p columns.}

\item{A}{Numeric. Number of subjects intended to be borrowed from external
data source.}

\item{s}{Integer. Number of strata for propensity score stratification (default 3).}

\item{a}{Numeric. Tuning parameter for the arctangent elastic function (must be >= 1, default 1).}

\item{alpha}{Numeric. Shape1 hyperparameter for the Beta prior (default 0.5).}

\item{beta}{Numeric. Shape2 hyperparameter for the Beta prior (default 0.5).}

\item{za}{Numeric. Significance level for credible intervals (default 0.05 for 95\% CrI).}

\item{K}{Integer. Number of samples from the posterior distribution (default 10000).}

\item{double_adj}{Logical. If TRUE (default), both baseline and outcome adjustment. If FALSE, baseline only.}

\item{show_plots}{Logical. If TRUE, returns diagnostic plots in \code{plots} element (default FALSE).}
}
\value{
A list containing \code{n_s1}, \code{n_s0}, \code{ne_s1}, \code{ne_s0},
  \code{r_s1}, \code{r_s0}, \code{r_sat}, \code{r_eca}, \code{v}, \code{r},
  \code{A_eff}, \code{PPP}, \code{gPPP}, \code{gamma}, \code{mean.theta},
  \code{var.theta}, \code{CrI}, \code{Width}, \code{theta_s}, \code{theta_o},
  and optionally \code{plots} (list of ggplot objects) when \code{show_plots = TRUE}.
}
\description{
Applies the Bayesian Dynamic Borrowing double-adjustment method for single-arm
trials with binary outcomes. The single-arm trial is augmented with external
data. No treatment variable is used.
}
\examples{
\donttest{
set.seed(1)
n0 <- 50
n1 <- 30
X <- matrix(rnorm((n0 + n1) * 2), ncol = 2)
Z <- c(rep(0, n0), rep(1, n1))
Y <- c(rbinom(n0, 1, 0.4), rbinom(n1, 1, 0.35))
bdb_single_arm_bin(Y = Y, Z = Z, X = X, A = 10, s = 2, a = 1, K = 500, show_plots = FALSE)
}
}
\references{
Farjat, A., Ji, Y., Kaiser, A., Lu, C., Pap, A., Potts, J., Wang, M.
A Comprehensive Bayesian Double-Adjustment Approach to Dynamic Borrowing of
External Data. Manuscript in progress.

Wang, C., Li, H., Chen, W.C., Lu, N., Tiwari, R., Xu, Y. and Yue, L.Q., 2019.
Propensity score-integrated power prior approach for incorporating real-world
evidence in single-arm clinical studies.
Journal of biopharmaceutical statistics, 29(5), pp.731-748.
}
\seealso{
\code{\link{bdb_single_arm_cont}}, \code{\link{bdb_two_arms_bin}},
  \code{\link{bdb_two_arms_cont}}, \code{\link{elastic_function}}
}
